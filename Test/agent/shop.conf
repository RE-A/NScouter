# Scouter Java Agent — shop-app
# 키 이름은 javap 로 scouter.agent.Configure 의 public 필드를 확인한 것만 사용한다.

# 오브젝트 식별 (NScouter 에이전트 목록 / XLog 점 색상의 기준)
obj_name=shop-app
obj_type=tomcat
obj_name_auto_pid_enabled=false

# 콜렉터 접속
net_collector_ip=scouter-collector
net_collector_tcp_port=6100
net_collector_udp_port=6100

# XLog 전송
# 하한을 0으로 두지 않으면 빠른 트랜잭션이 아예 전송되지 않는다.
xlog_lower_bound_time_ms=0
# 테스트 환경에서는 표본 추출 없이 전부 보낸다.
xlog_sampling_enabled=false

# 프로파일 (SQL / apicall 스텝은 기본 수집. profile_off 가 마스터 스위치)
profile_off=false
profile_step_max_count=1024
profile_connection_open_enabled=true
profile_fullstack_service_error_enabled=true
profile_fullstack_sql_error_enabled=true
profile_fullstack_apicall_error_enabled=true

# 메서드 프로파일 — 프로파일 뷰 검증용으로 서비스 계층만 후킹
hook_method_patterns=com.nscouter.test.shop.service.*,com.nscouter.test.shop.repository.*

# JDBC 후킹 설정 없음
# PostgreSQL(org.postgresql.jdbc.PgPreparedStatement / PgStatement)은 Scouter 에이전트의
# 기본 후킹 대상이라 hook_jdbc_* 를 지정하지 않아도 sqlCount / sqlTime 과 SQL 프로파일이 수집된다.
# (SQLite 를 쓸 때는 기본 대상이 아니라서 직접 지정해야 했다.)

# 분산 트랜잭션 전파 (gxid / caller)
trace_interservice_enabled=true
trace_response_gxid_enabled=true

# 성능 카운터
counter_enabled=true

# 인터랙션(토폴로지) 수집. 기본값이 꺼짐이라 켜야 호출 관계가 쌓인다 (F-40)
counter_interaction_enabled=true

# JMX 카운터. datasource(ConnActive/ConnIdle/ConnMax)는 HikariCP MBean 을 읽어
# tomcat_ds 서브오브젝트로 올린다. 기본값이 꺼짐이라 켜야 한다.
jmx_counter_enabled=true

# SQL 리터럴 치환 — 실환경(CJFW)에서 프로파일 SQL 이 '@{1}' 형태로 오는 원인이다.
# 켜면 에이전트가 리터럴을 빼내 파라미터로 따로 보낸다 (EscapeLiteralSQL):
#   문자열 → '@{n}'   숫자 → @{n}   (n 은 1부터, 나온 순서)
# 그 형태를 화면에서 채워 보여줄 수 있는지 검증하려고 여기서도 켠다 (B-1).
profile_sql_escape_enabled=true
